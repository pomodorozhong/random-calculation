[project]
name = "hole-depth"
version = "0.1.0"
description = "Estimate hole depth by timing a dropped stone"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "ipykernel>=7.1.0",
]
